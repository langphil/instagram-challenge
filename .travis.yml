language: ruby
rvm:
  - "2.4.1"

services:
  - postgresql

script:
  - RAILS_ENV=test bundle exec rake db:migrate --trace
  - bundle exec rspec

before_script:
  - psql -c 'create database pgapp_test;' -U postgres
